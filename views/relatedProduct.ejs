<!-- 相關商品 -->
<section class="container bg-white mt-3">
    <h2 class="pt-3 related text-center"><strong>相關商品</strong></h2>
    <div class="row">
        <% relatedProducts.forEach(function(relatedProduct) { %>
            <div class="col-3">
                <a id="prodCard-<%= relatedProduct.prod_id %>"
                    href="/product/<%= relatedProduct.prod_id %>/<%= relatedProduct.spec_id %>">
                    <div class="card">
                        <img src="<%= relatedProduct.dir %>/<%= relatedProduct.filename %>" alt="產品圖片"
                            class="w-100 rounded">
                        <div class="ps-3">
                            <h4 class="mt-3"><b>
                                    <%= relatedProduct.prod_name %>
                                </b></h4>
                            <p>
                                <%= relatedProduct.spec_name %>
                            </p>
                            <div class="priceIcon">
                                <span class="mt-4" style="font-size: 1.5em;">
                                    NT <%= relatedProduct.price %>
                                </span>
                                <button class="btn btn-lg" data-prod_id="<%= prodId %>" onclick="collect()"
                                    data-spec_id="<%= specId %>">
                                    <i class="bi bi-heart"></i>
                                </button>
                                <button class="btn btn-lg" data-prod_id="<%= prodId %>" onclick="cart()"
                                    data-spec_id="<%= specId %>">
                                    <i class="bi bi-cart-plus"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </a>
            </div>
            <% }) %>
    </div>

    <div class="center mt-5 ">
        <div class="pagination">
            <% for(var i=1; i <=4; i++) { %>
                <span><a href="/<%= i%>">
                        <%= i%>
                    </a></span>
                <% } %>
        </div>
    </div>

</section>